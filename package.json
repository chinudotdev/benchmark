{
  "name": "gpu-benchmark",
  "version": "1.0.0",
  "description": "LLM GPU inference benchmark + cost calculator",
  "bin": "src/index.ts",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile gpu-benchmark",
    "bench": "bun run src/index.ts run",
    "summarize": "bun run src/index.ts summarize"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "yaml": "^2.4.5"
  },
  "devDependencies": {
    "@types/bun": "^1.1.12",
    "typescript": "^5.7.0"
  }
}
